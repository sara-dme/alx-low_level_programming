#include "main.h"

/**
 * print_binary - print the binary
 * @n: bin to beprinted
 */
void print_binary(unsigned long int n)
{
	unsigned long int x;
	int i;

	if (n == 0)
	{
		_putchar('0');
		return;
	}
	if (n == 1)
	{
		_putchar('1');
		return;
	}
}
