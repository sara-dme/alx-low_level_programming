#include "main.h"

/**
 * add : add two integer and returns the result
 * @a : int to add to b
 * @b : int to add to a
 * Return : sum of a and b
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
