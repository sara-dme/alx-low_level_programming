#include "main.h"

/**
 * _strlen - returns the length of a string.
 * @*s: char variable
 * Return: length of a string
 */
int _strlen(char *s)
{
	int i;
	int len;

	i = 0;
	while (*s)
	len = i + 1;
	return (len);
}
